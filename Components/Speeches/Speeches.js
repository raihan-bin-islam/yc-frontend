import React from 'react';
import styles from './speeches.module.scss'

const Speeches = () => {
    // classnames
    const { speechContainer } = styles


    return (
        <section className={speechContainer}>
            
        </section>
    );
};

export default Speeches;